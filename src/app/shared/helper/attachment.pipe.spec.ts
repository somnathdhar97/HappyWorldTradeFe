import { AttachmentPipe } from './attachment.pipe';

describe('AttachmentPipe', () => {
  it('create an instance', () => {
    const pipe = new AttachmentPipe();
    expect(pipe).toBeTruthy();
  });
});
